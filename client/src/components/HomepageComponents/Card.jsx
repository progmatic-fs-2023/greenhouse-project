import CardHeader from './CardHeader';
import CardContent from './CardContent';

function Card() {
  return (
    <div className="card">
      <CardHeader />
      <CardContent />
    </div>
  );
}

export default Card;
